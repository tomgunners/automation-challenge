{
  "name": "web",
  "scripts": {
    "pretest": "rimraf allure-results allure-report playwright-report test-results && ts-node scripts/allure-setup.ts",
    "test": "playwright test",
    "report:allure": "allure generate allure-results -o allure-report --clean && allure open allure-report",
    "report:playwright": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "@types/node": "^25.0.3",
    "allure-commandline": "^2.28.0",
    "allure-playwright": "^2.14.0",
    "rimraf": "^6.1.2",
    "ts-node": "^10.9.2"
  }
}